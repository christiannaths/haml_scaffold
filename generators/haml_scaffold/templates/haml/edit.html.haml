%h1 Editing <%= singular_name %>

- form_for @<%= singular_name %> do |f|
  = render :partial => 'form', :locals => { :f => f }
  .field= f.submit "Update"
%p
  = link_to 'Show', @<%= singular_name %> 
  |
  = link_to 'Back', <%= plural_name %>_path
